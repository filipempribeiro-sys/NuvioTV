<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Android 12+ uses the platform splash window attributes directly.
        The launcher theme already inherits Theme.MyApplication, so no
        AndroidX postSplashScreenTheme attribute is required.
    -->
    <style name="Theme.MyApplication.Launcher" parent="Theme.MyApplication">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/app_logo_mark</item>
    </style>
</resources>
