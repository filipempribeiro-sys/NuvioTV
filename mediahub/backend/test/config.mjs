import assert from"node:assert/strict";import{mediaHubConfig,validateProductionConfig}from"../src/config.js";assert.deepEqual(validateProductionConfig(mediaHubConfig({MEDIAHUB_ENV:"production",MEDIAHUB_PUBLIC_BASE_URL:"http://example.test"})),["MEDIAHUB_PUBLIC_BASE_URL must use HTTPS in production","MEDIAHUB_DATABASE_URL is required in production"]);assert.deepEqual(validateProductionConfig(mediaHubConfig({MEDIAHUB_ENV:"production",MEDIAHUB_PUBLIC_BASE_URL:"https://media.example",MEDIAHUB_DATABASE_URL:"postgresql://db"})),[]);console.log("MEDIA•HUB production config test passed");
