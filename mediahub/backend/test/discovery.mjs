import{spawn}from"node:child_process";import assert from"node:assert/strict";const port=18082,base=`http://127.0.0.1:${port}`,child=spawn(process.execPath,["src/server.js"],{cwd:new URL("..",import.meta.url),env:{...process.env,PORT:String(port),MEDIAHUB_PUBLIC_BASE_URL:base},stdio:"inherit"});const sleep=ms=>new Promise(r=>setTimeout(r,ms));try{await sleep(400);const d=await fetch(`${base}/.well-known/mediahub`).then(r=>r.json());assert.equal(d.product,"MEDIA•HUB");assert.equal(d.version,"1");assert.equal(d.apiBaseUrl,`${base}/v1`);for(const k of["accounts","profiles","devices","qrPairing","sync","liveTv"])assert.equal(d.capabilities[k],true);console.log("MEDIA•HUB discovery contract test passed");}finally{child.kill();}
